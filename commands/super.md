module.exports = function main() {
this.embeds = [{
  
  "title": "PLEASE Read updates FFS",
      "description": "SuperStream breaks when the main source of the API fucks up something. Cloudstream can't solve this issue, which is already posted in <#737729080492818543>. FFS read those before reporting the issue.\n\n**When the owner of the source fixes the issue**, it'll be fixed in SuperStream too. So don't report it.\n\nUse **SoraStream** instead, which is a very stable source with a lot of video and subtitle options.",
      "color": 16711680
  
  }]
}
